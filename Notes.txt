Python 
--------
>> It is a high-level, interpretted, OOP programming language.

Human written 
|
|(Compiled) (SyntaxErrors)
|
Byte code (common) 011011
|
|(interpret) (Runtime)
|
Machine code

extension --> .py
command to execute python file --> python filename.py

cli --> Command line interface

terminal --> cmd, powershell, git bash...

Print 
--------
>> to show a data at the output.
Ex: print(data, end="\n")


Input 
-------
>> To prompt data from user.
>> It always returns String.
Ex: input(expStr)

Syntax 
--------
>> Grammer of python.
>> Case sensitive.(a is not A)
>> Indendation Matters. (whitespace to define block of code)

Comments 
--------
VS code --> ctrl + /
>> Comments are ignored by python.
>>makes the code readable.
>>to disable a code snippet temp.

 Single line comment --> #
 Multiple line comments --> ''' '''

Variables 
-----------
it is a named memory that stores an data.
varName=data

Naming Rules 
----------------
>> can contain letters, numeric, _
>> cannot start with numeric.
>> no reserved keywords.

Datatype 
---------
To find datatype --> type(data).
Int --> Whole Numbers. 4,-4,0,1
Float --> Decimal Numbers. 5.22323,3.14

String --> Sequence of Characters.'' "".
Immutable 
>> Indexed and Ordered.
I'm Leo
"I said "Hi""

Operations:
Concatination --> str1+str2
Repetition --> str*2

''' ''' --> Multi line strings

In built methods 
------------------
.upper()
.lower()
.title()
.capitalize()

.find(subStr) -1
.count(subStr)

.split() str --> list
.join(iter)

.isdigit()
.isalpha()
.isalnum()

.startswith(subStr)
.endswith(subStr)

Formatted String --> f" {v1}  {v2} "

Escape Char:
\n --> line breaker
\t --> tab

Boolean
-----------
True/False

Type Casting
--------------
Converting an objects datatype into another.
int(obj)
str(obj)
float(obj)
bool(obj)

Operators 
-----------
>> It is a symbol or keyword that performs operations on operands(values).

Arithmetic 
------------
+ add
- sub 
* mul 
/ div 
// floor div 
% modulus (reminder) 10%13 --> 0 --> 10
** exponentiation --> 2**2 = 4

BODMAS

Comparison (Boolean)
-------------
==
!=
<
>
<=
>=

Logical (Bool ---> Bool)
----------
and --> True --> Both are True. False --> otherwise
or --> False --> Both are False. True --> otherwise.
not --> inverts a bool. T->F, F->T

Assignment 
-----------
= assign
+= add assign
-= 
*=
/=
//=
%=
**=

Bitwise 
---------
& --> AND 
| --> OR 
~ --> NOT (~n=-(n+1))
^ --> X-or (1 --> bits are not equal) (0 --> bits are equal)
Right shift --> num>>pos
Left Shift --> num<<pos

id --> return unique int to identify the objects.
Mutable --> List, Set, Dict.(no reuse)
Immutable --> Int, Float, Str, Tuple.(reuse)

Membership
-------------
in, not in

Identity
----------
is, is not 
to identify an object.

Iterable Object 
-----------------
>> An object that can return its own members 1 by 1.
Ex: String, list, tuple, dict, set...

Sequence 
----------
To find len of a seq --> len(obj)
>> Ordered collection of data.
Ex: String, list, tuple...

Indexing and Slicing  
---------------------
index starts with --> 0
negative indexing --> -1
varName[ind]

Slicing 
---------
>> Extracting part of a sequence.

seq[start:]
seq[:end]
seq[start:end]
seq[start:end:step]
seq[::-1]

Control Flow
---------------
Conditional Clause
Looping Statement

Conditional Clause 
--------------------
if statement 
---------------
syntax:
--------
if condition:
    block of code
condition --> expression that returns Bool.

True --> execute the block. False --> skip the block of code

elif statement 
-----------------
syntax:
---------
elif cond:
    block of code

True --> execute the block. False --> skip the block of code

else Statement 
---------------
syntax 
---------
else:
    block of code

False --> execute the block. True --> skip the block.

Nested If 
------------
if cond1: 
    if cond2:  
        block of code

Loop
-----------
>> It executes a block of code for multiple times.

While --> Condition based loop
For --> Sequence based loop 

While (When you don't know the no of iteration)
-------
Syntax:
----------
while cond:
    block of code

>> keep repeating the block of code for multiple times as long as the condition is True.

For Loop (number of iteration is known(preferable))
--------
Syntax:
----------
for varName in Sequence:
    block of code

Range --> to create a sequence with sequential Numbers 
-----------------------------------------------------------
range(len) --> range(5) --> [0,1,2,3,4]
range(start,end) --> range(14,20) --> [14,15,16,17,18,19]
range(start,end,step) --> range(2,20,1)

Control Statememnts 
---------------------
break --> stop the loop
continue --> skip the current iteration.

Nested Loops 
------------------
loop1(5):
    loop2(5):
        block of code

Data structures 
---------------
>> It is a way of organizing and storing data.

List --> Sequence
------
>> [ele1,ele2.....]
>> Ordered and Indexed
>> Mutable(Updatable)
>> It allows duplicates

In built methods 
--------------------
.append(ele)
.insert(pos,ele)
.extend(Iterables)

.remove(ele) --> first occur
.pop(ind)
.pop() --> removes last element

.index(ele) --> finds index number of an element
.count(ele)

.reverse()
.sort()
.clear()

Tuple 
--------
>> (ele,ele....)
>> Ordered, Indexed 
>> It allows duplicates
>> Immutable

In built methods 
------------------
.index(ele) --> finds index number of an element
.count(ele)

Unpacking 
------------
person=("Leo",23,"SASE")
name=person[0]
age=person[1]
role=person[2]

name,age,role=person

Set 
-----
>> {ele,ele.....}
>> Unordered, indexed 
>> Mutable 
>> It does not allow duplicates 

In built methods 
--------------------
.add(ele)
.remove(ele)
.clear()

.union(s1)
.intersection(s1)
.difference(s1)

Dict 
-----
>> {key:val,key:val......}
>> Ordered. But not indexed
>> keys cannot be duplicated But vals can.
>> Mutable.

In built methods
-------------------
.pop(key)
.clear()

.keys()
.values()
.items()

Functions 
-------------
It is a reusable block of code that performs a specific task.

Pre defined --> print, input, type, len.....
User Defined --> defined by yourself


syntax
----------
def fnName():
    block of code

Calling(invoking) a function --> fnName()

Parameter and Argument 
-------------------------
P --> these are the variables you create in function defenition without assigning values

A --> values you are passing to those parameters.

Positional Arguments 
-----------------------
>> Order matters

Keyword Arguments 
-------------------
>> assigning values for parameters by using p's name.

Default Argument 
------------------
>> assigning default value for a parameter.

Variable length Argument
--------------------------
*arg --> multiple vals.. tuple
**karg 

return keyword 
----------------
>> stop the function
>> give back value to the caller.

Scope 
------
>> it means the region where a particular name(var, func, class..) can be accessed.

Global (accessible anywhere) (global)
-------
a name created outside of any function.

Local (accessible only within that function)
---------
a name created inside a function.

Enclosing 
----------
variables declared in outer function is accessible in inner function

Built in Scopes(accessible anywhere without restrictions)
----------------
>> it is for in built functions.
Ex: print, input....

File Handling 
----------------
Process of creating, reading, writing, modifying and managing files. Ex: txt, py, html, xml,  binary, json...

To open a file --> open(path,mode)
To close a file --> .close()

To read content from file --> .read(size)
To read the content line by line --> .readline()
To read the content line by line --> .readlines()

To know cursor pos --> .tell()
To change the cursor pos --> .seek(pos)

.writable()
.write()

Mode 
-------
Read --> "r"
Write --> "w" (over writing) (create)
Append --> "a" (add) (create)
Create --> "x"

with statement 
---------------
with open() as f:
    block of code 

Error Handling 
----------------
>> error is a problem that stops the code from running properly.

Syntax Errors
---------------
>> It occurs when you break syntax of python.
Ex: SyntaxError, IndentationError...

Runtime Errors (can be handled) 
------------------
>> These are caught during runtime.
Ex: TypeError, ZeroDivisionError, ValueError...

Exception Handling 
-------------------

try:
    block of code 
except NameOfError:
    handling block 
.
.
finally:
    block of code